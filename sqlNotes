SQL: structured query language  (https://www.thoughtspot.com/sql-tutorial/)
----------------------------------

DATA : collection of information 
Database: container/place where we can store data
          1.Relational Database(tables)
          2.Non-Relational Database(documents/objects)
          3.Graph Databases(graphs)

DBMS(software):  database management system
       flat like , tree like , graph like, rdbms(tables)


table/entity- rows and columns
             - column(attribute/field)
             - row(record)

Data types: 1.Numeric
            2.character
            3.Data and Time
            4.Logical Data Types


show databases;
use database_Name;
show tables;
desc table_Name/show fields in table_Name/show columns in table_Name;





DDL : data definition language
     ->create 
     ->alter
     ->rename
     ->drop
     ->truncate     

database:
---------
         1.to read : show databases;
         2.to create: create database database_Name;
         3.to delete: drop database database_Name;
         4.to use: use database_Name;  

Table:
------
      1.to create : 
            Datatypes: what type of data need to store
            constraint: provides extra information about column 
          create table student(
             col_NAME1 data_type constraint,
             col_NAME2 data_type constraint,

          )
      2.to alter a columns:
           1.add a column:
                 alter table table_Name 
                 add (col_NAME2 data_type constraint);

           2.Rename a column:
                alter table table_Name
                rename column oldColumnName to newColumnName 
          
           3.delete a column 
                alter table table_Name
                drop column ColumnName  

     3.to rename a table:
           rename table oldTableName to newTableName;
      
     4.to delete a table:
           drop table table_Name; (entire table will delete)
                 or
           truncate table tale_Name;(records will delete but table structure remains same) 
 
===================================================================================================

DML : Data Manipulation Language
      ->insert
      ->update
      ->delete 
      
insert : insert into tableName
         values(__,__,___,__);  // values count need to match with column count 
               or
          insert into tableName (col1,col2,col4)
         values(val1,val2,val4); 
           or
          insert into tableName 
          values(__,__,___,__),
          values(__,__,___,__),
          values(__,__,___,__);

update : update tableName set colName=value ,colName=value where condition;

delete : delete from tableName where condition ; // based on condition it will delete record
         delete from tableName ;//it will delete entire table records 
  

==================================================================================================
DQL: Data Query language
     (Reading data)
      ->select
      ->joins 
      ->sub queries and co related sub queries  



    select * from table_Name; -- to read all records
    select colName1,colName2 from table_Name;---to read respective records

aliasing : to change column Name
--------
      select colName  as customName from table_Name;
  
Operators:
---------
         Arithmetic Operator :+ ,-,*,/,% or mod()

literals: data/value
--------
         1.number---1,2, 
         2.String----''
         3.Data and Time -- ''
        
concatenation: combining/joining data
------------- 
             :||(double vertical pipes)
             :concat(,)
   Q.WAQTP MY NAME IS COL 
   A. select 'MY NAME IS '||FIRST_NAME from hr.employees;
                        or
      select concat('MY NAME IS ',FIRST_NAME) from hr.employees;

order by: order by clause will print data in an order 
--------
   Q.WAQTP employee salaray based on ascending order  
   A. select first_name,salary from hr.employees order by salary; // default is ascending order
                or
      select first_name,salary from hr.employees order by salary asc 
                or
      select first_name,salary from hr.employees order by salary desc

   Q.WAQTP all the emp details in the ascending order based on dept number and descending order based on salary
   A.select * from hr.employees order by DEPARTMENT_ID , salary desc; 

where: where is a clause used for condition 
------
      1.Relational Operator(<,>,>=,>=,=,!= or <>)
      2.Logical operator(and ,or , not)
      3.special operator(in,between ,is ,like )

     Q.WAQTP whose salary is more than 3000;
     A.select * from scott.emp where sal>3000;
     Q.WAQTP whose salary is more than 2000 in descending order;
     A.select * from scott.emp where sal>2000 order by sal desc;
     Q.WAQTP employee who joined before 1980;
     A.select * from scott.emp where hiredate<'MM-DD-YYYY'
     Q.WAQTP employee whose job is manager and ename is blake
     A.select * from scott.emp where job='MANAGER' and ename='BLAKE';
     Q.WAQTP employee whose job is manager or president
     A.select * from scott.emp where job='MANAGER' or job='PRESIDENT';
                  or
       select * from scott.emp where job in ('MANAGER','PRESIDENT');
     Q.WAQTP employees whose salaray in the range of 2000 to 3000 
       select * from scott.emp where sal>=2000 and sal<=3000;
                or
       select * from scott.emp where  sal between 2000 and 3000;
     Q.WAQTP whose employees commission is null
       select * from scott.emp where  comm is null;
      
     Q.WAQTP employee whose job is not manager or president
       A.select * from scott.emp where job!='MANAGER' or job!='PRESIDENT';
                  or
       select * from scott.emp where job not in ('MANAGER','PRESIDENT');
     Q.WAQTP employees whose salaray in the  out of range  2000 to 3000 
     select * from scott.emp where sal<=2000 and sal>=3000;
                or
     select * from scott.emp where  sal not between 2000 and 3000;
     Q.WAQTP whose employees commission is not null
       select * from scott.emp where  comm is not null;
     Q.WAQTP whose employee name starts with A
       select * from scott.emp where ename like 'A%' ;
     Q.WAQTP whose employee job ends with K
       select * from scott.emp where job like '%K' ;
     Q.WAQTP whose employee name consist of E
       select * from scott.emp where ename like '%E%' ;
     Q.WAQTP whose employee name consist E in 4th index position
      select * from scott.emp  where ename like '___E_' ;

FUNCTIONS
---------
     -functions are pre - defined operations 
   1.Aggregate Functions : multi row functions -- function will work on all records and
                         returns  single output   10 records-output-1
                       1.Sum() -numbers
                       2.max() -strings ,numbers, date 
                       3.min() -strings ,numbers, date 
                       4.count()-strings, numbers,date
                       5.avg()  -numbers

  2.Scalar Functions:  single row functions --- functions will work on every single record 
                       and returns output for each record  10 records-output is 10
                     1.concat() - concatenation
                     2.initcap()-it will make first character as capital and remaining as lowercase
                     3.replace()-it will helps to replace 
                                -3 args(1-data,2-word to be replace 3- new word) 
                                -2 args(1-data, 2-word to be deleted) 
                     4.substr() -it will help to get a part of string
                                -3 args(1-data,2-start position ,3-end position) 
                                -2 args(1-data,2-start position --it will extract until last char) 
                     5.length() - to find length of data
                     6.upper() - converts into upper case
                     7.lower()  - converts into lower case

   3.Date Functions: 
                     select now() - get current date and time
                     select current_date- gets current data
                     select current_time-gets current time
                     select current_timestamp - gets current timestamp 
   4.Math Functions:
       1.abs()-> returns the absolute value of a 9*/9+number
       2.ceil()-> returns the smallest integer greater than or equal to a number
       3.floor()-> returns the largest integer less than or equal to a number
       4.round()-> returns the nearest integer to a number
       5.sign()-> returns the sign of a number
       6.mod()-> returns the remainder of a number divided by another number
       7.Power()/pow()-> returns the value of a number raised to the power of another number
                   
Subquery:
--------
          - used to find data that belongs to itself or other table
          - writing query inside another query 

Q.WAQTP the location details of blake
A.select loc from scott.dept where DEPTNO=( select DEPTNO from scott.emp where ename='BLAKE') ;

Q.WAQTP the employee details whose belongs to Chicago location
select * from scott.emp where deptno=(select deptno from scott.dept where loc='CHICAGO');

Q.WAQTP the manager of blake
select * from scott.emp where empno=(select mgr from scott.emp where ename='BLAKE');

Q.WAQTP the employee details whose salary similar with ford
select * from scott.emp where sal=(select sal from scott.emp where ename='FORD');

Q.WAQTP the employee details whose belongs to SALES department
select * from scott.emp where depno=(select deptno from scott.dept where dname='SALES');


group by:
----------
          group by is a clause used to group the records
Q.WAQTP the count of employee whose belongs to each department
A.select count(*) as count, deptno from scott.emp group by deptno ;

Q.WAQTP the count of department , whose getting salary more than 2000
select count(*) as count , deptno from scott.emp where sal>=2000 group by deptno order by deptno ;

position of keywords
      select 
      from
      where
      group by
      having
      order by

order/flow/execution:
      from
      where
      group by
      having
      select
      order by 
 

where clause-- filter based on condition upon records
having clause- filter based on condition upon groups
             - to use having clause , we need to use group by clause 
 
Q.WAQTP the count of employee and department whose having salary more than 2000
 and department need to have more than 1 person 
select count(*) as count , deptno from scott.emp where sal>=2000 group by deptno having count(*)>1 order by deptno ;


limit : limit is a keyword which limits the output
SELECT *   FROM tutorial.us_housing_units  order by midwest limit 10





Joins:
------
    - joining or merging one or more tables
    - used to combine rows from two or more tables based on a related column 
      between them.

Types of joins:
---------------
           1.cross join
           2.inner join/equi join
           3.outer joins
                     1.left outer join
                     2.right outer join
                     3.full outer join
           4.self join

1.cross join/cartesian join:
--------------------------         
         -In this join , it returns each and every record of the table is going to be
          matched and unmatched.
         -In Cross Join, if table A contains 10 records and table B contains 4 records,
          then the cross join will return 40 records. 
         -whenever we use cross join, will get correct data as well as incorrect data 

CMD: select * from tableA cross join tableB;
     select * from tableA,tableB;

2.inner join/equi join:
----------------------
        - it returns only the matched records.
        - In inner join, if table A contains 10 records and table B contains 4 records,
          then the inner join will return 4 records.
        - for equi joins, proper condition should be given in where clause.

CMD: select * from tableA inner join tableB where condition;
     select * from tableA,tableB where condition;

Note : condition must be matching in both the tables.

Q.WAQTP list of employee whose belongs to dallas location
select e.ename , d.loc 
from scott.emp e inner join scott.dept d 
on e.DEPTNO=d.DEPTNO
where d.loc='DALLAS';



3.Outer join:
-------------
    - it returns matched records as well as unmatched records.
    types:
    ------
        1.left outer join/left join
        2.right outer join/right join
        3.full outer join/full join

  1.left outer join:
  =================
           -it returns all the records from left table and 
            matched records from right table.
          -if there is no matched records in right table, will return null values.


   2.right outer join/right join:
   ==============================
           -it returns all the records from right table and 
            matched records from left table.
          -if there is no matched records in left table, will return null values.




3.outer join/full outer join/full join:
=======================================
           -it returns all the records from both the tables.
           -it returns matched records as well as unmatched records.
           -it can't achieve by using mysql syntax.

CMD: select * from tableA full outer join tableB on condition; // not supported by mysql
                   or
     select * from tableA left join tableB on condition
                      union
     select * from tableA right join tableB on condition;
       


4.self join:
------------
          - joining the table with itself. 
          - without aliasing we can't use self join.  
          - to compare rows within same table
          - to find the relationship between rows within same table.

CMD:  select * from tableA a join tableA b on a.column=b.column;

Q.WAQT find manager of each employee
select e1.ename as employee, e2.ename as manager

from scott.emp e1 ,scott.emp e2

where e2.empno=e1.mgr;
